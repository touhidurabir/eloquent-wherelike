<?php

return [

    /*
    |--------------------------------------------------------------------------
    | WHERE LIKE SQL OPERATOR
    |--------------------------------------------------------------------------
    |
    | The where like method sql operator . By default set to 'LIKE' but can be
    | updated to according to database driver . For example : for database
    | driver postgresql, it should be set to 'ILIKE' .
    |
    */
    'operator' => 'LIKE',
];
